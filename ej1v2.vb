Public Module Program
	Sub Main()
	  Dim A, B, C, cen, dec, nnum As Integer
	  'Dim B As Integer
	  'Dim C As Integer
	  'Dim cen As Integer
	  'Dim dec As Integer
	  'Dim nnum As Integer
	  A = 2
	  B = 3
	  C = 4
	  'cen = A * 100
	  'dec = B * 10
	  nnum = (A*100) + (B*10) + C
		Console.WriteLine(nnum)
	End Sub
End Module